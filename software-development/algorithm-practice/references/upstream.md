# Upstream Provenance

Adapted from: https://github.com/beyondaprilzjl-lab/leetcode-skill

Commit inspected: `f09452b feat(lc-review): 复习时为每道题附上 leetcode.cn 链接`

Original shape: three Claude Code skills (`lc-go`, `lc-done`, `lc-review`) plus Markdown templates.

Local changes:

- collapsed into one Hermes umbrella skill `algorithm-practice`;
- default data directory changed to `~/.hermes/data/study/leetcode`;
- WSL/Linux `date -d` commands are the default;
- Obsidian is optional, not assumed;
- Hot 100 checklist includes LeetCode CN links/slugs where known.
